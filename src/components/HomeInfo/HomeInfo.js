import React from 'react';

const HomeInfo = (props) => {

    return (
        <div>
            <h2>hi</h2>
            {/* {
                data.map(breakfast => <p>{breakfast?.name}</p>)
            } */}
        </div>
    );
};

export default HomeInfo;